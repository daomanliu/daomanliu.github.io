# 2026-08-07

## 博客调整：恢复旧登录页样式 + 显式退出登录按钮

- LoginView 恢复为旧版设计：全屏 bg.png 背景 + 左上角 logo（改为链回首页）+ 右侧半透明卡片；去掉演示账号提示，登录后跳 /admin
- BlogLayout 顶栏：未登录显示「登录」链接；已登录显示「后台 + 昵称 + 退出登录按钮」（原来藏在下拉里，用户找不到）
- 踩坑：vite preview 运行时构建会因 dist 被占用导致 emptyDir 失败，需先停掉 preview 进程再 build

## 后台编辑器加宽 + 恢复可折叠分屏
- admin 路由加 `meta.wide`，BlogLayout 宽布局（后改为 max-width:none、无 padding，全屏）
- AdminView 恢复旧 EditorView 的「折叠编辑/折叠预览」互斥折叠分屏（ColumnWidthOutlined + pane-label），编辑区 flex:1 撑满；发布提示 alert 可关闭

## AdminView 完全恢复旧笔记本布局（用户截图要求）
- 左侧 280px 文章列表 sider（可用菜单按钮收起为 0）；右侧：大标题无边框输入框 + 按钮组（信息/折叠编辑/折叠预览/保存/导出/删除）、标签栏、分屏编辑
- 文件名/日期/摘要收进「信息」可切换的 meta-bar（新建时默认展开，因为要填 slug）
- 发布流程提示 alert 可关闭且记忆（localStorage blog-admin-tip-closed）
- 注意：用户截图显示 daomanliu.github.io 上部署的还是旧笔记本应用（旧 dist），提醒其重新 push 新构建
- 顶栏改通栏：头像+博客名贴最左，首页/后台/退出贴最右（header-inner 去掉 900px 限宽）
- 左侧文章列表：草稿悬浮显示删除按钮（popconfirm），已修改文章悬浮显示撤销按钮；删除正在编辑的条目时同时关闭编辑器
- 用户不想每次本地构建：新增 .github/workflows/deploy.yml（push main → npm ci/build → deploy-pages），README 补充三种发布方式：手动推 dist / Actions 自动部署(推荐) / 只传 md+手动改 posts.json 应急
