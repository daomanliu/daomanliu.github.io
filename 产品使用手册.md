欢迎使用 **Markdown 笔记本**！这是一款运行在浏览器中的笔记工具，让你用 Markdown 语法专注写作，同时实时看到排版后的效果，并支持数学公式、标签整理与数据导入导出。

本手册面向普通使用者，带你从登录到日常使用的全部操作。

---

## 一、登录

1. 打开应用后，你会看到登录页面。
2. 输入账号与密码（可使用演示账号）：
|账号|密码|
|---|---|
| 刘道满 | 123456 |
| demo | demo |
3. 点击 **登录** 按钮。登录成功后会自动进入笔记本主界面，并为你生成第一篇欢迎笔记。

> 提示：登录状态会被记住，刷新页面无需重新登录；使用公共电脑时请记得退出。

---

## 二、主界面一览

登录后的界面分为三块：

- **顶栏**：应用标题、全局搜索框、标签管理 / 导出 / 导入 / 新建按钮，以及右上角你的头像与昵称。
- **左侧列表**：按更新时间排序的笔记列表，可按顶栏搜索框筛选。
- **右侧编辑区**：笔记标题、标签栏，以及「编辑 / 预览」左右分屏。

顶栏最左侧的折叠按钮可以收起 / 展开左侧列表，给你更大的写作空间。

---

## 三、创建与编辑笔记

1. 点击顶栏的 **＋ 新建笔记**，会自动创建一篇「未命名笔记」并打开编辑区。
2. **标题**：在编辑区顶部输入框填写；留空时列表中会显示为「未命名笔记」。
3. **正文**：在左侧编辑框中书写 Markdown。
4. **实时预览**：右侧会随输入即时渲染效果，无需手动刷新。
5. **自动保存**：所有修改会在你停止输入约 0.3 秒后自动保存到本地，不必担心丢失。
6. **折叠编辑区 / 预览区**

编辑区顶部有两个折叠按钮：

- **折叠编辑**：编辑区缩成只剩标题栏，预览区占满整个右侧，方便专注查看效果。
- **折叠预览**：预览区缩成只剩标题栏，编辑区占满整个右侧，给你最大的写作空间。

点击「展开」即可恢复左右分屏。两个区域互斥折叠，始终至少有一个保持展开。
### 支持的 Markdown 语法


- **粗体**、*斜体*、~~删除线~~
- [链接](https://swu.edu.cn)
- 图片：
![图片](https://www.swu.edu.cn/images/weixintupian_20250403181622.png)

- 代码块：
```javascript
const hello = 'world'
console.log(hello)
```

> 引用块

### 公式（LaTeX）

行内公式：$E = mc^2$，质能方程。

块级公式：

$$
f(z) = \sum_{n=-\infty}^{\infty} a_n (z-z_0)^n
$$
$$
a_n = \frac{1}{2\pi i} \oint_C \frac{f(\zeta)}{(\zeta - z_0)^{n+1}} d\zeta
$$
---

## 四、收藏笔记

在编辑区顶部的星标按钮上点击即可收藏 / 取消收藏。被收藏的笔记在左侧列表中会显示金色星标，方便你快速找到重要内容。

---

## 五、搜索

在顶栏中间的搜索框中输入关键字，左侧列表会实时筛选出匹配项。搜索范围包含：

- 笔记标题
- 笔记正文
- 标签名称

清空搜索框即可恢复全部列表。

---

## 六、标签管理

标签帮助你按主题归类笔记。

1. 点击顶栏 **标签管理**，打开标签弹窗。
2. **新增**：在底部输入框填写标签名，回车或点「添加标签」。
3. **重命名**：点击某标签的「重命名」，修改后保存；该标签在所有笔记中同步更新。
4. **删除**：点击「删除」并确认；该标签会从标签库及所有笔记中一并移除（笔记本身不会删除）。

在 **编辑区** 的标签栏中，也可以直接为当前笔记添加 / 移除标签（输入后回车确认）；新标签会被自动收录到标签库。

---

## 七、导出笔记

1. 点击顶栏 **导出**，弹出笔记选择框。
2. 默认已全选当前列表，可按需取消勾选，或先在弹窗内搜索筛选。
3. 选择导出格式：
   - **JSON**：完整保留标题、正文、标签、时间等结构化数据，便于日后重新导入。
   - **Markdown**：导出为 `.md` 纯文本文件，每篇笔记前以 `# 标题` 开头，适合在其他 Markdown 工具中打开。
4. 点击 **导出选中**，浏览器会下载一个以日期命名的文件（如 `markdown-notes-2026-07-11.json`）。

---

## 八、导入笔记

1. 点击顶栏 **导入**，选择本地文件。仅支持 `.json`、`.md`、`.markdown`。
2. 系统检测到笔记数量后，会让你选择导入方式：
   - **新建文档**（推荐）：把导入的笔记合并进来，已存在（同 id）的不会重复。
   - **覆盖当前**：用导入内容替换掉当前全部笔记（谨慎使用）。
3. 导入完成即自动打开第一篇笔记。

> JSON 导入兼容其他应用导出的格式（自动识别 `content` / `body` / `text` 等字段），缺失字段会补全为默认值。

---

## 九、删除笔记

在编辑区顶部的 **删除** 按钮上点击，确认后即可删除当前笔记。删除后列表会自动跳转到其他笔记。

> 删除操作不可撤销，重要笔记建议先导出备份。

---

## 十、退出登录

1. 点击右上角头像 / 昵称，打开菜单。
2. 选择 **退出登录** 并确认。
3. 退出后本地内存中的笔记会被清空，下一次用其他账号登录只会看到该账号自己的内容。

---

## 十一、常见问题（FAQ）

**Q：我的笔记存在哪里？**
A：保存在当前浏览器的 `localStorage` 中，按账号隔离。换浏览器、清缓存或用别的电脑都无法看到，请定期导出备份。

**Q：换了设备 / 清除了浏览器数据，笔记没了怎么办？**
A：本地存储无法恢复，请使用「导入」功能从之前导出的文件恢复。

**Q：为什么公式没正常显示？**
A：请检查公式语法，行内用 `$` 包裹，
块级用`$$`包裹，且内部表达式符合 LaTeX 规范；渲染失败时公式会以红色提示。

**Q：多个账号会看到彼此的笔记吗？**
A：不会。每个账号数据独立存储，退出登录即清空当前账号在内存中的内容。

**Q：忘记密码了？**
A：当前为演示账号，密码见登录页提示；正式接入后端后由相应系统管理。

---

> 祝你写作愉快 ✍️ 如有更多功能需求，欢迎反馈